# hvac-simulator-java 项目全局规则

本文件中的规则适用于整个仓库。除非用户明确给出更高优先级的指令，否则所有任务都必须遵守这些规则。

## 规则一：Git 开发与交付流程

### 1. 核心原则

仓库建立后，开发默认遵循：

同步 `main` → 创建短期任务分支 → 编码、测试、提交 → 推送任务分支 → AI 提供 PR 材料 → 用户创建并合并 PR → 用户通知 AI“已合并” → AI 同步本地 `main` 并安全清理本地任务分支。

`main` 只保存已经完成、测试通过、可以演示或交付的内容。禁止直接在 `main` 上开发、提交或推送。

如果当前目录尚未初始化 Git、没有 `main`、没有远程仓库或缺少必要权限，必须如实报告当前状态，不得伪造分支、提交、推送或 PR 已经完成。仓库初始化、首个基线提交和远程绑定应作为明确的独立步骤处理。

默认分工：

- AI 负责检查工作区、同步本地 `main`、创建任务分支、实现、测试、提交和推送任务分支。
- AI 负责提供 PR 创建链接、建议标题、说明、测试结果和未验证事项。
- 用户负责创建、审核和合并 PR，除非用户明确改变本次任务的分工。
- 用户通知“已合并”后，AI 才同步本地 `main` 并清理本地任务分支。

### 2. 开始任务

开始修改前必须：

1. 读取 `AGENTS.md`、`PROJECT_GUIDE.md` 和 `PROJECT_STATUS.md`。
2. 检查 Git、分支、远程和工作区状态。
3. 确认没有混入其他任务的改动。
4. 获取最新远程状态，确认本地 `main` 可以仅快进到 `origin/main`。
5. 从同步后的 `main` 创建短期任务分支。
6. 分支创建完成后才修改文件。

示例：

```powershell
git fetch --prune origin
git switch main
git merge --ff-only origin/main
git switch -c feature/java-simulator
```

如果本地 `main` 与远程分叉、存在未推送提交或工作区包含来源不明的改动：

- 不得自动覆盖、重置、变基或合并；
- 不得创建包含无关提交的任务分支；
- 必须先报告证据和影响，再采用经确认的隔离方案。

### 3. 分支和任务边界

分支按任务性质命名：

- `feature/<name>`：新增功能；
- `fix/<name>`：缺陷修复；
- `perf/<name>`：性能优化；
- `refactor/<name>`：代码重构；
- `docs/<name>`：文档或代码注释；
- `test/<name>`：测试建设；
- `chore/<name>`：构建、配置和维护。

一个分支只处理一个明确任务。禁止顺手提交无关格式化、重构、配置或临时文件。

暂存时必须明确列出文件，不得使用 `git add .` 随意暂存全部内容：

```powershell
git add -- <file1> <file2>
git diff --cached --name-only
git diff --cached --check
```

### 4. 设计与计划

新模块、新输出契约、核心算法、数据流程、并发、持久化或跨组件功能必须先确认设计和范围，再实施。

纯注释、纯文档修正、拼写修复和不改变行为的简单格式调整可以跳过单独设计，但不能跳过范围检查、必要验证和 Git 流程。

### 5. 提交规范

提交信息采用：

`type(scope): 简短说明`

常用类型包括 `feat`、`fix`、`perf`、`refactor`、`docs`、`test` 和 `chore`。

每个提交必须：

- 内容单一并能独立解释目的；
- 不包含生成目录、临时文件、敏感配置或无关改动；
- 提交前完成与改动相匹配的测试或检查；
- 如实记录未执行、失败或跳过的验证。

不得使用 `--no-verify` 绕过仓库已有检查。仓库尚未提供 Hook 或 CI 时，不得描述为已经具备这些防护。

### 6. 推送和 PR 材料

任务完成且验证通过后，推送远程任务分支：

```powershell
git push -u origin <branch-name>
```

AI 应向用户提供：

- Base 和 Compare 分支；
- PR 创建链接、建议标题和可复制说明；
- 已实现内容与明确排除范围；
- 测试命令、结果、跳过项和未验证项；
- 冲突检查和无关文件检查结果；
- 生产代码注释检查结论。

AI 交付 PR 材料后，当前状态是“等待用户创建并合并 PR”，不能声称整个任务已经完成。

### 7. 合并后清理

只有用户明确通知“已合并”后，AI 才能：

1. 获取最新远程状态并清理失效引用；
2. 切换到本地 `main`；
3. 仅快进同步 `origin/main`；
4. 确认任务分支已合并且工作区干净；
5. 使用安全删除方式删除本地任务分支；
6. 如使用独立 worktree，在确认无未提交改动后再清理。

如果本地 `main` 不能仅快进，立即停止并报告分叉，不得自动强制处理。

### 8. 禁止操作

除非用户明确授权并确认恢复方案，否则禁止：

- `git push --force` 或 `git push --force-with-lease`；
- `git reset --hard`；
- 强制移动 `main`；
- 删除未合并分支；
- 覆盖用户未提交的改动；
- 擅自丢弃提交；
- 直接推送远程 `main`；
- 提交密码、Token、私钥、本机敏感配置或未脱敏数据。

## 规则二：测试与结果验证

### 1. 基本要求

提交前至少运行与改动直接相关的定向测试。涉及生产 Java 代码时，还应运行仓库实际提供的完整测试入口；Maven 工程建立后优先使用 Maven Wrapper，未建立 Wrapper 前按仓库当前有效命令执行。

测试或交付说明必须如实记录：

- 实际执行的命令；
- 通过、失败和跳过情况；
- 未执行项目及原因；
- 自动化测试不能覆盖的人工确认范围。

测试失败时不得把任务描述为可合并或已完成。

### 2. 测试隔离和可复现性

普通自动化测试不得依赖个人目录、临时文件、真实外部服务、当前系统时间或不可控随机输入。

仿真测试应固定：

- 起止时间和时间步长；
- 建筑、内部负荷、HVAC 和气象参数；
- 随机种子或确定性气象输入；
- 时区、单位和输出字段顺序；
- 运行所需的测试资源版本。

外部参考文件必须先转化为仓库内可追踪的测试夹具、基准结果或明确的生成步骤，不能长期依赖个人电脑上的临时绝对路径。

### 3. Python 与 Java 基准对照

Gaia 1.0 是第一阶段 Java 转换的行为参考。转换实现必须使用固定场景对照关键结果，至少覆盖：

- 室内温度和室外温度；
- 冷负荷；
- 冷机及系统功率；
- 冷机 COP；
- 关键启停边界和零负荷场景；
- 时间序列长度、时间戳和输出字段。

浮点结果不得直接使用精确相等。每类指标必须根据公式规模和业务意义定义清晰容差，并在测试中说明依据；禁止使用一个过宽的全局容差掩盖公式、单位或状态更新错误。

对照结果必须区分：

- 逐时间步结果；
- 汇总值或统计量；
- 允许的数值误差；
- 确认属于预期差异的实现变化。

未经设计确认，不得在 Java 转换过程中顺便修正 Python 模型的公式、参数或物理假设。发现疑似错误时，应保留对照证据并单独报告。

### 4. 边界和异常测试

算法和状态更新至少检查与改动相关的边界：

- 空调开启、关闭和死区边界；
- 零负荷、最小负荷、额定负荷和容量上限；
- 非法时间范围、非正时间步长和缺失参数；
- NaN、无穷值、除零和不合理物理参数；
- 数组长度、时间序列对齐和最后一个时间步；
- 功率、负荷、COP 和温度结果的合理范围。

### 5. 图表验证

温度、负荷、功率和 COP 图表必须来自本次仿真真实结果，不得使用与计算链路无关的手工或随机数据。

自动化检查至少覆盖：

- 图表文件已生成且非空；
- 时间范围和数据点数量正确；
- 必需数据序列存在；
- 标题、坐标轴、单位和图例完整；
- 输出目录和文件命名稳定且可重复生成。

自动化检查不能代替视觉验收。交付领导确认前，还应人工检查曲线是否清晰、遮挡是否可接受、中文是否正常显示以及异常尖峰是否来自真实计算。

## 规则三：用途优先的代码注释

### 1. 注释目标

新增或修改生产代码时，应从维护者角度检查注释是否足以说明：

- 当前类或方法负责什么；
- 它在完整仿真流程中的位置；
- 输入从哪里来、输出交给谁；
- 为什么采用当前公式、边界或状态更新方式；
- 有哪些无法从代码命名直接看出的限制、副作用和异常含义。

注释数量不是目标。应优先保证信息准确、长期有效和容易理解。

### 2. 类级和关键方法注释

新增核心仿真类时，应添加简洁、直白的中文类级注释，说明职责、上下游关系以及不负责的相邻职责。

关键方法是否需要注释由职责决定，不由 `public`、`protected` 或 `private` 可见性决定。承担以下职责的方法属于关键方法：

- 仿真入口或主要阶段；
- 负荷、功率、COP、气象或热过程公式；
- 状态推进、设备启停和时间步更新；
- 输入输出模型之间的重要转换；
- 边界限制、数值保护、异常转换或降级处理；
- 文件生成、结果导出或图表生成。

关键方法注释应按实际情况说明业务步骤、输入语义、单位、下游调用、返回结果、限制和副作用。

### 3. 数值仿真专项说明

以下信息无法由类型和命名完整表达时，必须在最接近实现的位置使用中文说明：

- 公式来源、物理含义和适用范围；
- 输入、输出及中间量的单位；
- 热量、负荷、功率和温差的正负号约定；
- 时间步长与积分或状态更新方式；
- 初始条件、额定值、上下限和死区；
- 简化模型、经验系数和物理假设；
- 截断、钳制、插值、舍入和浮点容差的原因；
- 与 Gaia 1.0 保持一致但可能需要后续核验的行为。

这部分是针对当前数值仿真项目的适配要求，不应描述成参考项目原文已经逐项规定的内容。

### 4. 不需要添加的注释

以下内容通常不需要注释：

- 普通 getter、setter；
- 一眼可以看懂的赋值、判空和简单委托；
- 框架注解或固定模板的基础含义；
- 已由清晰命名完整表达的简单代码；
- 与附近说明重复的内容；
- 把 Java 语句逐行翻译成中文；
- 使用“当前任务”“本次修改”等过程历史代替长期职责。

不得用注释掩盖职责混乱、命名不清或过度复杂的代码。

### 5. 修改代码后的注释检查

每次修改生产代码后必须检查：

1. 新增或改变的职责是否已经说明；
2. 核心公式、状态变化、单位和符号约定是否清楚；
3. 关键方法的输入、输出、上下游和限制是否清楚；
4. 原有注释是否仍与当前实现一致；
5. 是否出现重复、逐行翻译、未来设想或无实际信息的注释；
6. 是否需要同步更新 `PROJECT_GUIDE.md` 或 `PROJECT_STATUS.md`。

交付时应如实说明已完成注释检查；不能仅用注释数量或是否存在 Javadoc 判断质量。

## 规则四：项目信息分层与维护

### 1. 信息职责

项目相关信息按以下职责保存：

- `AGENTS.md`：长期固定的 Git、测试、注释和文档管理规则，不记录具体任务进度。
- `PROJECT_GUIDE.md`：稳定项目地图，包括项目定位、长期边界、主要组件、数据流程、运行入口、输出契约和文档导航。
- `PROJECT_STATUS.md`：其所在 Git 版本可验证的当前阶段、完成项、未完成项、阻塞、风险、技术债和下一步。
- `docs/superpowers/specs`、`docs/superpowers/plans`：经确认的具体任务设计、取舍、实施步骤和验证方案，属于任务级历史。
- 当前会话：正在执行的命令、临时错误、排查过程和尚未进入 Git 的状态。
- Codex Memories：只保存用户偏好和跨项目通用经验，不保存本项目提交号、测试数量、文件进度、本地端口、进程和临时路径。

### 2. 任务开始时的读取顺序

1. 读取 `AGENTS.md`，确认固定工作规则。
2. 读取 `PROJECT_GUIDE.md`，定位项目边界、核心流程和文档入口。
3. 读取 `PROJECT_STATUS.md`，确认当前完成范围、未完成项和风险。
4. 只在当前任务需要时读取相关设计、计划、代码和测试，不无目的加载全部历史文档。

如果文档与当前代码冲突，必须核验代码、自动化测试和已合并决策，不能直接沿用旧描述。

### 3. 更新规则

- 项目定位、稳定边界、核心流程、运行入口、输出契约或文档导航变化时，在同一任务分支更新 `PROJECT_GUIDE.md`。
- 项目阶段、完成项、未完成项、阻塞、风险、技术债或下一步变化时，在同一任务分支更新 `PROJECT_STATUS.md`。
- 新模块、核心算法、输出契约或数据流程变化时，先形成任务级设计和实施计划。
- 当前会话中的命令、日志和临时判断不得机械复制到项目文档，只提炼长期有效且有证据支持的结论。
- 文档与代码一起经过分支、检查、提交和 PR 流程。

### 4. 证据、冲突和过期处理

- 当前代码、自动化测试和已合并决策是核验行为的直接证据。
- `PROJECT_GUIDE.md` 负责稳定结构，`PROJECT_STATUS.md` 负责动态状态，两者不得重复维护同一事实。
- 任务设计和计划记录当时上下文；与当前实现不一致时视为历史演进记录，不能未经核验批量改写。
- 缺少代码、测试、提交或有效文档证据的事项必须标记为“计划中”“部分完成”“未验证”或“待核验”，不得推测为已完成。
- 项目目录尚未进入 Git 时，所有本地文件都必须明确视为未提交状态，不能称为已经进入项目基线。

### 5. 安全边界

以下信息不得写入项目指南、项目状态、任务设计或实施计划：

- 密码、Token、私钥、数据库凭据和未脱敏数据；
- 个人电脑上的临时进程、端口和敏感环境信息；
- 微信或其他临时目录的绝对路径；
- 临时备份路径；
- 无法验证来源的完成声明和运行结果。

只有属于仓库公开配置契约的示例路径、端口和参数才能进入稳定文档。

### 6. 交付检查

每个任务交付前必须检查：

1. 是否需要更新 `PROJECT_GUIDE.md` 或 `PROJECT_STATUS.md`；
2. 稳定信息和动态状态是否进入正确文件；
3. 是否混写了历史设计、当前事实和未来计划；
4. 是否包含敏感信息、本机临时状态或无证据结论；
5. 新任务能否通过三个入口文件快速建立可靠上下文；
6. 已实现、已测试、计划中和未验证的内容是否清楚区分。
