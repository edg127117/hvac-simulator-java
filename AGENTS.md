# hvac-simulator-java 项目规则

本文件适用于整个仓库，只保存长期硬约束、Skill 触发条件和文档路由。用户当前指令优先；个人全局规则继续生效。

## 1. 建立任务上下文

每个任务先读取本文件，再按需读取：

- 涉及项目定位、模型版本、参考基线、架构、运行入口或稳定事实时，读取 [`PROJECT_GUIDE.md`](PROJECT_GUIDE.md)；
- 涉及当前实现、未完成项、风险、技术债、下一步或计划时，读取 [`PROJECT_STATUS.md`](PROJECT_STATUS.md)；
- 模型、参数、气象、随机行为、状态推进、输出、CSV、图表或参考资产：读取 [`simulation-verification.md`](docs/development/simulation-verification.md)；
- Java 生产代码：读取 [`code-comments.md`](docs/development/code-comments.md)；
- Gaia 1.0、Gaia 1.1 或正式平台决策：从 `PROJECT_GUIDE.md` 导航到对应已确认设计。

只读取与当前任务直接相关的设计、计划、代码、测试和参考资产。历史文档不证明当前实现；证据不足必须标记“计划中”“部分完成”“未验证”或“待核验”。

## 2. Skill 触发

- 本仓库 Java 生产代码、公式、参数、气象、随机测量、状态推进、时间语义、输出、CSV、图表、模型版本、Python 参考或冻结基准发生变化时，必须使用仓库级 `hvac-simulation-verification` Skill。
- 创建、修改、重构或审查生产代码及注释时，必须使用个人全局 `code-comment-quality` Skill，并叠加本仓库 [`code-comments.md`](docs/development/code-comments.md) 的仿真专项要求。
- 涉及文件写入、分支、暂存、提交、推送、PR、worktree 或合并后清理时，必须使用个人全局 `safe-pr-delivery` Skill。
- Skill 不可用不免除本文件硬约束；仓库专项规则优先于通用 Skill。

## 3. 范围、设计与 Git

一个任务、分支和 PR 只处理一个明确问题。禁止顺手提交无关格式化、重构、配置、生成目录、运行输出或本机状态。

新模型版本、新模块、新输出契约、核心算法、数据流程、并发、持久化或跨组件功能必须先确认设计和范围。纯文档、注释、拼写和不改变行为的简单调整可跳过独立设计，但不能跳过范围、验证和 Git 流程。

修改前核对仓库、远程、分支、工作区和基线，从最新 `main` 创建短期任务分支。基线不明、`main` 分叉、工作区不干净或存在来源不明的变化时停止并报告；不得自动覆盖、重置、变基或带入无关工作。

暂存必须明确列出文件并检查暂存差异；禁止用 `git add .` 随意收集全部内容，禁止绕过检查、强推、直接在 `main` 开发或覆盖用户改动。只有用户明确通知“已合并”后，才能同步 `main` 并清理任务分支或 worktree。

## 4. 模型与项目硬边界

- 当前可运行实现是 Java 21、Maven Wrapper 和普通 Java CLI；Spring Boot、Vue、MySQL、TDengine、权限、任务管理和 MQTT 仍是待实施目标，不能写成当前能力。
- Gaia 1.0 是已实现模型版本；Gaia 1.1 当前只有接入设计，尚未形成稳定 Python 基准或 Java 实现。版本间参数、字段和语义必须显式隔离。
- Python 模型是相应版本忠实转换的行为参考。默认保持公式、参数、时间语义、单位、随机顺序和状态更新顺序。
- 发现原模型疑似错误时，先保存复现、参考输出和差异证据并建立独立任务；禁止静默修正公式、参数、单位或物理假设。
- 更新冻结基准、输出字段、时间语义、容差或参考资产必须单独设计并说明兼容影响，不能为了测试通过直接覆盖预期结果。
- 图表必须来自本次同一份真实仿真结果；自动化检查、Python/Java 一致性、视觉确认、物理合理性和性能验收是不同结论。
- 不因未来平台可能变大而提前拆仓库、微服务或创建尚未实现的模块规则。正式平台按已确认设计分阶段实施。
- 本仓库与 `iot-platform-demo` 独立；不得复制另一项目的业务范围、完成状态、路径、端口或数据事实。

## 5. 验证与注释

可执行验证入口是 [`.agents/skills/hvac-simulation-verification/SKILL.md`](.agents/skills/hvac-simulation-verification/SKILL.md)，开发者原则见 [`simulation-verification.md`](docs/development/simulation-verification.md)。生产 Java 变化至少运行相关定向测试和完整 `.\mvnw.cmd test`；只有打包、资源、CLI 或交付产物变化时才补充 package、JAR、CSV 和图表验证。

基准模式按时间戳、行和字段逐项比较；容差必须有依据，不能掩盖差异。测试和交付必须记录实际命令、结果、跳过、未执行原因和人工确认范围。失败时不得描述为可合并。

Java 注释使用简洁中文，重点说明仿真职责、上下游、公式、单位、符号、时间步、状态副作用、随机顺序、模型版本和兼容原因；不逐行翻译，不记录任务历史，不把未来平台能力写成现状。

核心模型、公式、状态推进、随机行为和大范围重构检查完整变化文件与相关调用链；普通生产 Java 修改检查变化及受影响方法；纯注释、拼写和机械调整只核验变化附近。

纯文档整改只运行链接、关键术语、Skill、文件范围和 Git 差异检查，不机械运行无关 Java 测试或重新生成仿真产物。

## 6. 信息分层与交付

- `AGENTS.md`：长期硬规则和条件式路由；
- `PROJECT_GUIDE.md`：稳定定位、模型版本、基准、架构、运行入口和输出契约；
- `PROJECT_STATUS.md`：当前完成项、未完成项、风险、技术债和下一步；
- `docs/development`：开发者长期规范；
- `.agents/skills`：Codex 可执行的项目专项流程；
- `docs/superpowers/specs`、`plans`：任务当时的设计和实施历史；
- `reference/` 与测试资源：版本化参考资产和可复现基准；
- 当前会话：命令、错误和临时判断。

稳定目标、模型边界、核心链路、运行入口、输出契约或文档入口变化时更新 `PROJECT_GUIDE.md`；阶段、完成项、风险、技术债或下一步变化时更新 `PROJECT_STATUS.md`。不得在项目文档中写入密码、Token、私钥、未脱敏数据、个人临时路径或无证据完成声明。

交付前确认范围、Gaia 版本和忠实基准边界、实际验证、注释检查、文档同步、暂存文件、敏感信息、生成文件及 `git diff --check`；提交并推送后提供完整 PR 材料，不把等待用户创建和合并 PR 说成已进入 `main`。
