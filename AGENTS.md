# hvac-simulator-java 项目规则

本文件适用整个仓库，仅保存必载硬约束和按需文档路由。

## 1. 建立任务上下文

每个任务开始时依次读取：

1. 本文件；
2. [`PROJECT_GUIDE.md`](PROJECT_GUIDE.md)：稳定项目定位、模型版本、参考基线、实际架构、运行命令和文档入口；
3. [`PROJECT_STATUS.md`](PROJECT_STATUS.md)：当前实现、未完成项、风险和下一步；
4. 只读取与当前任务直接相关的设计、计划、代码、测试和参考资产，不批量加载历史文档。

按任务条件继续读取：

- 模型公式、状态推进、气象、参数、随机测量、输出字段、CSV、图表或参考基准：[`docs/development/simulation-verification.md`](docs/development/simulation-verification.md)；
- Java 生产代码：[`docs/development/code-comments.md`](docs/development/code-comments.md)；
- Gaia 1.0、Gaia 1.1 或正式平台：从 `PROJECT_GUIDE.md` 的文档导航读取对应已确认设计。

历史设计与计划记录当时决策，不证明当前已经实现。文档与代码冲突时核验当前代码、自动化测试和已合并决策；证据不足必须标记为“计划中”“部分完成”“未验证”或“待核验”。

## 2. 范围、设计与 Git 流程

一个任务、分支和 PR 只处理一个明确问题。禁止顺手提交无关格式化、重构、配置、生成目录、运行输出或本机状态。

新模型版本、新模块、新输出契约、核心算法、数据流程、并发、持久化或跨组件功能必须先确认设计和实施范围。纯文档、注释、拼写和不改变行为的简单格式调整可以跳过独立设计，但不能跳过范围、验证和 Git 流程。

开始修改前必须：

1. 读取三个项目入口文件并检查 Git、分支、远程和工作区；
2. 确认没有其他任务或来源不明的改动；
3. `git fetch --prune origin`；
4. 切换本地 `main`，仅使用 `git merge --ff-only origin/main` 同步；
5. 从最新 `main` 创建 `feature/`、`fix/`、`perf/`、`refactor/`、`docs/`、`test/` 或 `chore/` 短期分支；
6. 分支创建完成后才修改文件。

如果仓库、远程或权限不完整，或 `main` 分叉、基线不明、工作区不干净，停止并报告；不得自动覆盖、重置、变基或带入无关工作。

暂存必须明确列出文件：

```powershell
git add -- <file1> <file2>
git diff --cached --name-only
git diff --cached --check
```

禁止使用 `git add .` 随意暂存全部内容。提交信息采用 `type(scope): 简短说明`，每个提交内容单一、已完成匹配验证且不含敏感信息、临时文件或无关变化。不得使用 `--no-verify` 绕过现有检查，也不得把尚未存在的 Hook 或 CI 描述为已具备。

验证通过后推送任务分支，AI 提供 Compare 链接、PR 标题、说明、范围、测试、冲突和无关文件检查。默认由用户创建、审核和合并 PR；AI 交付后状态是“等待用户创建并合并 PR”，不能声称任务已经进入 `main`。

只有用户明确通知“已合并”后，才能获取远程状态、仅快进同步本地 `main`，确认分支已合并且工作区干净，再安全删除本地任务分支或 worktree。

除非用户明确授权并确认恢复方案，禁止：

- 直接在本地或远程 `main` 开发、提交或推送；
- `git push --force`、`git push --force-with-lease`、`git reset --hard`；
- 强制移动 `main`、删除未合并分支、覆盖用户未提交内容或擅自丢弃提交；
- 提交密码、Token、私钥、个人配置、临时依赖和未脱敏数据。

## 3. 模型与项目硬边界

- 当前可运行实现是 Java 21、Maven Wrapper 和普通 Java CLI；Spring Boot、Vue、MySQL、TDengine、权限、任务管理和 MQTT 仍是待实施平台目标，不能写成当前能力。
- Gaia 1.0 是已实现模型版本；Gaia 1.1 当前只有接入设计，尚未形成稳定 Python 基准或 Java 实现。不同版本的参数、字段和语义必须显式隔离。
- Python 模型是相应版本忠实转换的行为参考。默认保持公式、参数、时间语义、单位、随机顺序和状态更新顺序。
- 发现原模型疑似错误时先保存对照证据并建立独立任务；禁止在忠实转换中静默修正公式、参数或物理假设。
- 更新冻结基准、输出字段、时间语义、容差或参考资产必须单独设计并说明兼容影响，不能为了让测试通过直接覆盖预期结果。
- 图表必须来自本次同一份真实仿真结果；自动化检查、Python/Java 一致性、视觉确认、物理合理性和性能验收是不同结论，不得互相替代。
- 不因为未来平台可能变大而提前拆仓库、拆微服务或创建尚未实现的 engine、server、web 规则。正式平台按已确认设计分阶段实施。
- 当前仓库与 `iot-platform-demo` 是独立项目；不得复制另一项目的业务范围、完成状态、路径、端口或数据事实。

稳定模型、包职责、命令和输出契约以 `PROJECT_GUIDE.md` 为准。

## 4. 仿真验证与结果

涉及模型、参数、气象、随机行为、状态推进、输出或图表时，必须读取 [`simulation-verification.md`](docs/development/simulation-verification.md)。

生产 Java 变化至少执行相关定向测试和完整 `.\mvnw.cmd test`。涉及打包、资源或可执行交付时，还要分别验证 JAR、CSV 和图表。基准模式按时间戳、行和字段逐项比较；浮点容差必须有明确依据，不能用过宽全局容差掩盖差异。

测试和交付说明必须记录实际命令、成功、失败、跳过、未执行原因和人工确认范围。测试失败时不得推送为可合并 PR。纯文档整改不机械运行无关 Java 测试，但必须执行链接、关键语义、文件大小和 Git 差异检查。

## 5. Java 注释检查

修改 Java 生产代码前读取 [`code-comments.md`](docs/development/code-comments.md)。注释使用简洁中文，重点说明仿真职责、上下游、公式、单位、符号、时间步、状态副作用、模型版本和兼容原因；不逐行翻译，不记录任务历史，不把未来平台能力写成现状。

生产文件变化后检查完整文件和全部方法/构造器，包括私有关键方法，并复核已有注释时效。

## 6. 项目信息分层

- `AGENTS.md`：长期固定规则和条件式文档路由，不记录任务进度；
- `PROJECT_GUIDE.md`：稳定项目定位、模型版本、基准、架构、运行入口、输出契约和文档导航；
- `PROJECT_STATUS.md`：当前版本的完成项、未完成项、风险、技术债和下一步；
- `docs/development`：按需读取的长期仿真验证和注释规范；
- `docs/superpowers/specs`、`plans`：具体任务当时的设计和实施历史；
- `reference/` 与测试资源：版本化参考资产和可复现基准；
- 当前会话：命令、错误、临时判断和未进入 Git 的状态；
- Codex Memories：个人偏好和跨项目经验，不保存本项目提交号、测试数量、端口、进程和临时路径。

稳定目标、模型边界、核心链路、运行入口、输出契约或文档入口变化时更新 `PROJECT_GUIDE.md`；阶段、完成项、风险、技术债或下一步变化时更新 `PROJECT_STATUS.md`。文档与代码一起走分支、验证、提交和 PR 流程。

任何项目文档都不得写入密码、Token、私钥、数据库凭据、未脱敏数据、个人临时路径、端口/进程快照或无证据完成声明。

## 7. 交付检查

交付前确认：

1. 变化严格属于当前任务，无无关文件和提交；
2. Gaia 模型版本、忠实基准和禁止静默修模边界未被破坏；
3. 相关测试、产物或文档检查已执行并如实记录；
4. 生产 Java 注释检查已完成，或明确不涉及生产代码；
5. `PROJECT_GUIDE.md`、`PROJECT_STATUS.md` 是否需要同步更新；
6. `git diff --check`、暂存文件、敏感信息和生成文件检查通过；
7. 已提交、推送任务分支并提供完整 PR 材料。
